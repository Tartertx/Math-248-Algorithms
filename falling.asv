function falling()
    mass = input('How much does the ball weigh? ');
    airRes = input('How much (non-zero) air-resistance is there? ');
    startHeight = input('What is the starting height of the ball? ');
    t0 = input('What positive number would you like to use as an initial guess for the time at which the ball bounces? ');
    gravity = 9.8;

end